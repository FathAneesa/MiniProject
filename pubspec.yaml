name: aiwellness
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  google_fonts: ^5.0.0
  flutter_dotenv: ^6.0.0
  shared_preferences: ^2.2.2 
  fl_chart: ^0.69.0 # Added for charting capabilities

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/logo.png